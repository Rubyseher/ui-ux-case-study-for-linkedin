export default function ContextSection() {
  return (
    <section id="context" className="mb-24 scroll-mt-20">
      <h2 className="text-xs font-semibold text-gray-400 uppercase tracking-[0.12em] mb-5">
        Context
      </h2>
      <p className="text-[18px] text-gray-800 leading-relaxed max-w-2xl">
        LinkedIn is used by over 1 billion people to find jobs. But the moment
        you hit submit, you go blind. No visibility into what happens next, even
        though LinkedIn already has all the data.
      </p>
    </section>
  );
}
